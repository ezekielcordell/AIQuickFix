{
    "configurations": [
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "name": "Run AI Quick Fix Extension",
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "preLaunchTask": "npm: build",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "type": "extensionHost"
        }
    ],
    "version": "0.2.0"
}
